#ifndef _SORT_STRUCT_H
#define _SORT_STRUCT_H

#include <stdio.h>

#include "struct_subject.h"

void swap_struct(subject_t *subject_1, subject_t *subject_2);
void sort_array_by_density(subject_t *arr, size_t n);

#endif //_SORT_STRUCT_H